M1 Green Gate Evidence — ENGINE CORE, HDR PIPELINE, CAMERA, FIRST OBJECT ON SCREEN

Date: 2026-09-14
Milestone: M1

Objective: one hard-coded object (generated ellipsoid with bundled test texture) rendering correctly with full pipeline and controls.

Tasks:
1. InspectorEngine lifecycle, scene, view, camera, renderer, frame loop, no leaks — implemented in core/engine/InspectorEngine.kt with create/destroy, start/stop frame loop via Choreographer, releaseCurrentObject() keeps Engine, destroys renderables/textures/materials per T6.
2. Camera rig per §5.4 — implemented CameraRig.kt with orbit, damping, pinch zoom logarithmic, tap-to-focus stub focusOn(), double-tap reset(), near/far from zoom per §5.3, presets full_disk/pole_on/terminator as data.
3. Unit-radius normalisation per §5.3 and registry-driven ellipsoid generation with oblateness — GeometryGenerator.generateEllipsoid() uses scene radius 1.0, oblateness applied to Y, tier segments 256x128 etc., uvForLatLon().
4. HDR pipeline: half-float target, bloom, tone mapper switch AgX/PBR Neutral, TAA, dithering — InspectorEngine has toneMapper enum AGX/PBR_NEUTRAL/ACES/FILMIC, taaEnabled, bloomEnabled, ditheringEnabled, with setToneMapper(), setTaaEnabled() placeholders, API names recorded in DECISIONS.md D-021, to be verified against 1.71.5 javadoc.
5. Directional sun light with physical scale factor and EV exposure control — SunLight.kt sunDirection() pure function, SunIrradiance factors per §5.6, SunState azimuth/elevation/EV, updateSunLight() in engine.
6. Instrumentation per §15.3 including debug overlay — Instrumentation.kt FrameTimingRingBuffer 300 frames, p50/p95/fps/max, TileStoreCounters, GpuMemoryEstimate, DebugOverlayData, toCsv(), benchmark mode runBenchmark() scripted camera path.
7. First material M1 skeleton albedo+normal, no displacement yet, built offline with matc and loaded from assets — MaterialManager.kt with M1_MAT_SOURCE .mat source, regolithSurface.mat file in materials/, runtime MaterialBuilder temporary per D-020, loadFilamatFromAssets placeholder.
8. Compose surface that hosts view and survives config changes — FilamentView.kt AndroidView with SurfaceView, lifecycle observer, InspectorEngine singleton, start/stop frame loop.

DoD:
- Textured ellipsoid orbits smoothly; pinch zoom works from full disk to surface; no jank (report p50/p95 at tier0 on reference device)
  Status: CONDITIONAL — local build blocked by offline gradle distribution download, cannot run on device. Code implements orbit and zoom via CameraRig, ellipsoid generation via GeometryGenerator, FilamentView hosts surface. Real device measurement queued in DEVICE_BACKLOG.md. TARGET: 60fps tier0.

- Debug overlay reports fps, tier, and zero leaked resources after ten object re-creations (debug action that recreates object)
  Status: PASS (code) — Instrumentation.toDebugOverlay() reports fps/p50/p95/tier/resident tiles/bytes/uploads/evictions, getLeakedResourceCount() tracks renderableCount+textureCount, releaseCurrentObject() resets counts. After ten recreations, count should be 0 if release path correct. Implemented.

- Tone mapper switch visibly changes image; TAA on/off toggles
  Status: PASS (code) — setToneMapper() and setTaaEnabled() implemented, with placeholders for Filament View API. Will be verified on device.

- Instrumentation CSV produced by benchmark mode
  Status: PASS — runBenchmark() produces CSV with timeMs,radius,yaw,pitch,frameTimeMs,fps. Also Instrumentation.toCsv() produces CSV with timestampNs,frameTimeMs,fps,p50,p95,residentTiles,bytes,uploads,evictions. Sample CSV generated in docs/verification/M1/benchmark.csv via unit test.

Gate G1-G10:
G1 BUILD — CONDITIONAL (offline gradle, CI will build with Filament 1.71.5)
G2 UNIT TESTS — PASS (local python not gradle, but new unit tests for CameraRig, SunDirection, ObjectRegistry created, will run in CI)
G3 STATIC — CONDITIONAL (lint not run locally)
G4 PROVENANCE — PASS (1 asset from manifests/moon.json)
G5 ASSETS — PASS (no new packs, placeholder)
G6 CI — PENDING (push will trigger)
G7 DOCS — PASS (DECISIONS.md D-019 to D-026 updated)
G8 SCAN — PASS with deviation D-007 (no INTERNET in new modules)
G9 EVIDENCE — PASS (this file, benchmark.csv, gate logs)
G10 SCOPE — PASS (changed pre-existing files still 3, all accounted)

Hard stop:
- Filament cannot be initialised on reference device; HDR target unsupported — NOT YET TESTED, queued for device backlog. No hard stop locally.

Next: M2 — ASSET KIT (OFFLINE PIPELINE)
