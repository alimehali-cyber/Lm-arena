MUTATION PROOFS (item C, 2026-09-04) for CappedQuadIndexTest against QuadPatternIndex.capped

MUTATION 2 (min-separation dropped from combo check):
  capped at full budget equals brute force exactly: FAILURE -
  capped(full budget) must equal brute-force quad set expected:<74 quads> but was:<93>:
  mutated builder includes quads containing the injected 0.05-deg-separated pair
  ([8,31,40,41] etc., indices 40/41 = TIGHT_A/TIGHT_B) which brute force correctly excludes.

MUTATION 3 (global dedupe dropped):
  capped at full budget equals brute force exactly: FAILURE -
  no duplicates allowed expected:<74> but was:<296>

All other tests 154/0/0 before and after restore.
