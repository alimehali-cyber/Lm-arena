# HeroSkyProjectionTest — FIRST REAL EXECUTION (evidence)
# Captured: 2026-09-03, remediation pass.
# Method: one-off compile with kotlinc 2.4.10 + custom JUnit runner (same harness as the
# startracker suite) plus a minimal pure-Kotlin stand-in for androidx.compose.ui.geometry.Offset
# (only x/y/Zero members used). The Gradle/Android toolchain remains unavailable, so this is
# the pure-Kotlin subset behavior; Compose Offset semantics are trivial for these members.
#
# RESULT: 7 tests — 6 PASS, 1 FAILURE.
# The failure is testSouthernHemisphereEastWestOrdering_MirroredExpectation:
#   Actual: eastX=250.0, westX=750.0 (lat=-35), isMirrored=false, isSameAsNorth=true
# i.e. HeroSkyProjection does NOT mirror East/West in the southern hemisphere — the bug
# that docs/startracker/PHASE9_INTEGRATION_PATCH.md documents. That patch has never been
# applied; until it is (or the mirrored expectation is consciously rejected), this test
# failing is the documented, intended state.
#
# Full output:
PASS   HeroSkyProjectionTest.testAzimuthWraparoundDistance
PASS   HeroSkyProjectionTest.testHemisphereCenterFacing
PASS   HeroSkyProjectionTest.testHeroSkyProjectionCoordinates
PASS   HeroSkyProjectionTest.testNorthernHemisphereEastWestOrdering
PASS   HeroSkyProjectionTest.testScreenDistanceCylindricalWraparound
PASS   HeroSkyProjectionTest.testSouthernHemisphereActualBehavior_Diagnostic
FAILURE   HeroSkyProjectionTest.testSouthernHemisphereEastWestOrdering_MirroredExpectation
        -> AssertionError: Southern (mirrored expectation): East (90) should be right of
           center (x > 500.0), West (270) left of center. Actual: eastX=250.0, westX=750.0,
           isMirrored=false, isSameAsNorth=true.

Tests run: 7, Failures: 1, Errors: 0

# ============================================================================
# POST-FIX RUN (same day, same one-off harness): PHASE9 fix APPLIED.
# HeroSkyProjection.kt southern branch changed 0-az -> az-0 (unified relAz = az - facing).
# Diagnostic test updated to assert the fixed mirrored behavior per the patch doc.
# Result: 7/7 PASS, including testSouthernHemisphereEastWestOrdering_MirroredExpectation.
# Startracker suite re-run after the fix: 130 tests, 0 failures, 0 errors.
#
# Full post-fix output:
PASS   HeroSkyProjectionTest.testAzimuthWraparoundDistance
PASS   HeroSkyProjectionTest.testHemisphereCenterFacing
PASS   HeroSkyProjectionTest.testHeroSkyProjectionCoordinates
PASS   HeroSkyProjectionTest.testNorthernHemisphereEastWestOrdering
PASS   HeroSkyProjectionTest.testScreenDistanceCylindricalWraparound
PASS   HeroSkyProjectionTest.testSouthernHemisphereActualBehavior_Diagnostic
PASS   HeroSkyProjectionTest.testSouthernHemisphereEastWestOrdering_MirroredExpectation
Tests run: 7, Failures: 0, Errors: 0

# ============================================================================
# PASS 2 (2026-09-03, item R2-A2): test renamed and relabelled.
# testSouthernHemisphereActualBehavior_Diagnostic ->
# testHemisphereOrderingExactPositions_PositiveCheck
# (a POSITIVE CHECK pinning EXACT positions 0.25w/0.75w for BOTH hemispheres; NOT a
#  duplicate of the direction-inequality MirroredExpectation test; original buggy-era
#  history documented in the test comment.)
# Full main-harness run after rename (HeroSky now part of the standard 137):
#   Tests run: 137, Failures: 0, Errors: 0  (incl. all 7 HeroSkyProjectionTest tests)
